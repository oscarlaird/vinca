# Fish shell completions for vinca
# completions are stored in ~/.config/fish/completions/

# (1) If the last argument is a subcommand which takes a parameter complete that parameter (e.g. filepath or filter predicate)
# (2) If the last command returns a cardlist (sort / filter / findall) then do completions for cardlists.
# (3) If the last command returns a card (lc / find / any index number) the do completions for cards.
# (4) If there are no arguments yet. Complete all the commands and values available from __main__

